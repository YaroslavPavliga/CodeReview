// Найпростіший тест для перевірки, що Jest працює
describe('Basic Math Tests', () => {
  test('1 + 1 should equal 2', () => {
    expect(1 + 1).toBe(2);
  });

  test('2 * 3 should equal 6', () => {
    expect(2 * 3).toBe(6);
  });
});
