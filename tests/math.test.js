'EOF'
// Прості математичні тести
test('1 + 1 equals 2', () => {
  expect(1 + 1).toBe(2);
});

test('2 * 3 equals 6', () => {
  expect(2 * 3).toBe(6);
});

test('10 / 2 equals 5', () => {
  expect(10 / 2).toBe(5);
});
EOF
